<MainScreen>:
    BoxLayout:
        orientation: "vertical"
        spacing: 10
        padding: 10

        Button:
            text: "Detections"
            on_release: app.root.current = "detection"

        Button:
            text: "Calibrations"
            on_release: app.root.current = "calibration"

        Button:
            text: "Analyzer"
            on_release: app.root.current = "analyzer"
